register("worldLoad", () => {
  let playerList = getAllPlayers();
  if (playerListtoLowerCase().includes('necron')) {
    break;
  } else { 
    new Thread(() => {
      Thread.sleep(1);
      ChatLib.chat(“message”);
    }
  }
});
